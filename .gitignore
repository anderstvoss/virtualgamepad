# Rust build artifacts
/target
/target/

# Local environment files
.env
.env.*
!.env.example

# Secrets and credentials
*.pem
*.key
*.p12
*.crt
secrets.*
credentials.*
config.local.*
local.*
private.*
id_rsa*
id_ed25519*

# Logs and temporary files
*.log
*.tmp
*.bak
*.swp
*.swo

# Local notes / scratch / private context
notes/
scratch/
todo-private.md
private/
.local/

# Per-user agent auto-memory (project-local but not shared across
# contributors yet — promote specific memory types to tracked once
# conventions settle). The .agents/ directory itself is tracked via
# .agents/.gitkeep so the symlink target referenced in README.md
# exists in fresh clones.
/.agents/memory/

# OS/editor files
.DS_Store
Thumbs.db
.vscode/
.idea/

# Coverage/profiling
tarpaulin-report.html
coverage/
*.profraw
*.profdata

# Generated artifacts
dist/
build/
out/
