# EXP-<gate>-<sequence> — <question>

Status: not_run. Owner: unassigned. Date: unset. Gate: unset.

## Scope and hypotheses

Controller/personality/realization and the one question tested. State competing explanations and the architectural decision this can unlock.

## Prerequisites and revisions

Code SHA and dirty-state summary; corpus gitlink and checkout SHA; schema/generator/source versions; related baseline and ADR. List missing prerequisites explicitly.

## Test topology and controls

Fake or live environment; device/firmware alias; kernel/driver; SDL/Steam settings; timing/cadence; identity selection; physical/reference condition. Identify independent variables and held-constant inputs. Exclude private machine identifiers.

## Predeclared acceptance criteria

Exact commands/procedure; repetitions and run duration; deadline margin/load or streaming tolerances where relevant; expected protocol bytes/errors; required cleanup; criteria for mixed, failed, and blocked results. Complete this before executing.

## Artifact manifest

| Artifact | Origin (physical/derived/synthetic) | SHA-256 | Safe location/reference | Transformation/provenance |
| --- | --- | --- | --- | --- |
| Pending | Unset | Unset | Unset | Unset |

Private raw inputs remain outside Git. Record safe hashes and transformation versions, including identity/checksum changes. Expected fixtures must identify whether they are independent of implementation generation.

## Results by evidence axis

| Axis | Status | Run/evidence | Limitation |
| --- | --- | --- | --- |
| Deterministic protocol/provider | not_run | None | Unset |
| Kernel/driver | not_run | None | Unset |
| Physical comparison | not_run | None | Unset |
| SDL/HIDAPI/sensors | not_run | None | Unset |
| Steam | not_run | None | Unset |
| Audio/BT (if applicable) | not_run | None | Unset |

Retain every repetition and negative/mixed outcome. Include command exit codes and cleanup results. Do not use not_applicable without a reason.

## Interpretation and decision

What the evidence establishes, what it does not, alternative explanations, ADR link, next bounded action, and revisit trigger. Update GATE_STATUS.md only after this record is populated.
