# Historical implementation framework

This filename is retained to avoid broken external links. Its former
profile/session framework is superseded by
[CONTROLLER_NATIVE_API_SPEC.md](CONTROLLER_NATIVE_API_SPEC.md).

Current implementation work must preserve the controller-agnostic contract and
runtime, controller-owned realization data, explicit target selection, atomic
typed state updates, retryable commits, bounded output delivery, and typed
reverse events defined by the canonical specification.
