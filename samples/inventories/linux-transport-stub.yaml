fixture: virtualgamepad/v1
kind: backend-inventory
id: linux-transport-stub
notes: |
  Phase 10 transport-tier inventory: the linux-transport-{usb,bluetooth}
  families are plannable for `--goal hardware-faithful` but `open_session`
  still refuses live sessions. Real USB/Bluetooth gadget realization lands
  in Phase 11.
payload:
  entries:
    - backend_id: linux-transport-usb
      family: linux-transport-usb
      level: transport
      host_platform: linux
      supported_fidelity_tiers:
        - hardware-faithful
      notes:
        - phase-10 transport backend is plannable; live USB gadget realization lands in Phase 11
    - backend_id: linux-transport-bluetooth
      family: linux-transport-bluetooth
      level: transport
      host_platform: linux
      supported_fidelity_tiers:
        - hardware-faithful
      notes:
        - phase-10 transport backend is plannable; live Bluetooth gadget realization lands in Phase 11
