fixture: virtualgamepad/v1
kind: backend-inventory
id: macos-hid-stub
notes: |
  Phase 12 foundations inventory: the macos-hid family is plannable for
  identity-aware goals so the planner can select it and surface entitlement /
  system-extension prerequisites. This is planning + deployment-requirement
  reporting only — no device realization lands in Phase 12.
payload:
  entries:
    - backend_id: macos-hid
      family: macos-hid
      level: hid
      host_platform: macos
      supported_fidelity_tiers:
        - identity-aware
      notes:
        - "phase-12 macos-hid backend is planning-only; no device realization yet"
        - "deployment requirement: a notarized DriverKit system extension and the matching app entitlement must be approved before realization"
