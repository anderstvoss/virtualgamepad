fixture: virtualgamepad/v1
kind: session-scenario
id: provider-panic
profile_id: dualsense
notes: |
  Phase 7 runtime scenario: declare a provider-panic failure on the fake
  backend so opening the session surfaces a BackendOpenFailed error
  instead of taking down the process. The manager-isolation invariant
  is that this error is reported cleanly and the demo binary exits
  non-zero without crashing or hanging.
payload:
  session:
    session_id: 34
    profile_id: dualsense
    fidelity_tier: identity-aware
    backend_level: hid
    host_platform: linux
  backend:
    backend_id: fake-backend
    family: linux-uhid
    host_platform: linux
    supported_fidelity_tiers:
      - identity-aware
    supported_output_functions:
      - rumble
      - haptics
      - lighting
      - player-indicators
      - trigger-effect
      - audio
    failures:
      - provider-panic
  steps: []
